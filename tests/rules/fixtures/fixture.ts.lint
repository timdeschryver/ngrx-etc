import { createAction } from '@ngrx/store'
import { createActionWithCorrelation } from 'ngrx-etc'

// empty createAction

const action = createAction('customers load')
                            ~~~~~~~~~~~~~~~~                          [action-hygiene]

const action = createAction('[Customers]: load')
                            ~~~~~~~~~~~~~~~~~~~                       [action-hygiene]

const action = createAction('[Customers] load')

// props createAction

const action = createAction('customers load', props<{ page: number }>)
                            ~~~~~~~~~~~~~~~~                          [action-hygiene]

const action = createAction('[Customers]: load', props<{ page: number }>)
                            ~~~~~~~~~~~~~~~~~~~                       [action-hygiene]

const action = createAction('[Customers] load', props<{ page: number }>)

// function createAction

const action = createAction('customers load', ( page: number ) => ({ number }))
                            ~~~~~~~~~~~~~~~~                          [action-hygiene]

const action = createAction('[Customers]: load', ( page: number ) => ({ number }))
                            ~~~~~~~~~~~~~~~~~~~                       [action-hygiene]

const action = createAction('[Customers] load', ( page: number ) => ({ number }))

// empty createActionWithCorrelation 

const action = createActionWithCorrelation('customers load')
                                           ~~~~~~~~~~~~~~~~            [action-hygiene]

const action = createActionWithCorrelation('[Customers]: load')
                                           ~~~~~~~~~~~~~~~~~~~         [action-hygiene]

const action = createActionWithCorrelation('[Customers] load')

// props createActionWithCorrelation 

const action = createActionWithCorrelation('customers load', props<{ page: number }>)
                                           ~~~~~~~~~~~~~~~~            [action-hygiene]

const action = createActionWithCorrelation('[Customers]: load', props<{ page: number }>)
                                           ~~~~~~~~~~~~~~~~~~~         [action-hygiene]

const action = createActionWithCorrelation('[Customers] load', props<{ page: number }>)

// function createActionWithCorrelation 

const action = createActionWithCorrelation('customers load', ( page: number ) => ({ number }))
                                           ~~~~~~~~~~~~~~~~            [action-hygiene]

const action = createActionWithCorrelation('[Customers]: load', ( page: number ) => ({ number }))
                                           ~~~~~~~~~~~~~~~~~~~         [action-hygiene]

const action = createActionWithCorrelation('[Customers] load', ( page: number ) => ({ number }))

[action-hygiene]: Action type does not follow the good action hygiene practices, use "[Source] Event" to define action types
